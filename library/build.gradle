apply plugin: 'com.android.library'

android {
  compileSdkVersion 20
  buildToolsVersion "20.0.0"

  defaultConfig {
    applicationId "com.github.fernandodev.easyratingdialog.library"
    minSdkVersion 8
    targetSdkVersion 20
    versionCode 1
    versionName "1.0"
  }
  lintOptions {
    abortOnError false
  }
}
